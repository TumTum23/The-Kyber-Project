import { Signer } from 'ethers';
import { MockProvider } from './MockProvider';
declare type Fixture<T> = (signers: Signer[], provider: MockProvider) => Promise<T>;
export declare const loadFixture: <T>(fixture: Fixture<T>) => Promise<T>;
export declare function createFixtureLoader(overrideSigners?: Signer[], overrideProvider?: MockProvider): <T>(fixture: Fixture<T>) => Promise<T>;
export {};
